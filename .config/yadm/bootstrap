#!/bin/sh

chmod 700 ~/.cache ~/.config

. $(dirname $0)/bootstrap_utils --load

__load_module decrypt_secrets
__load_module nvm
__load_module xcode
__load_module brew
__load_module macos

  # TODO:
  # check if dependencies are not installed and try to automaticall install them

. $(dirname $0)/bootstrap_utils --unload
